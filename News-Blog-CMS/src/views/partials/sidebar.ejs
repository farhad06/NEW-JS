<div id="sidebar" class="col-md-4">
  <!-- search box -->
  <div class="search-box-container">
    <h4>Search</h4>
    <form class="search-post" action="/search" method="GET" autocomplete="off">
      <div class="input-group">
        <input type="text" name="search" class="form-control" placeholder="Search .....">
        <span class="input-group-btn">
          <button type="submit" class="btn btn-danger ms-2">Search</button>
        </span>
      </div>
    </form>
  </div>
  <!-- /search box -->
  <!-- recent posts box -->
  <div class="recent-post-container">
    <h4>Recent Posts</h4>
    <% latestNews.forEach(function(news){ %>
    <div class="recent-post">
      <a class="post-img" href="">
        <img src="/upload/<%= news.image %>" alt="" />
      </a>
      <div class="post-content">
        <h5><a href="/single/<%= news._id %>"><%= news.title %></a></h5>
        <span>
          <i class="fa fa-tags" aria-hidden="true"></i>
          <a href='/category/<%= news.category.slug %>'><%= news.category.name %></a>
        </span>
        <span>
          <i class="fa fa-calendar" aria-hidden="true"></i>
          <%= new Date(news.createdAt).toLocaleDateString('en-GB', { day: '2-digit', month: 'long', year: 'numeric' }).replace(/\s/g, ', ') %>
        </span>
        <a class="read-more" href="/single/<%= news._id %>">read more</a>
      </div>
    </div>
    <% }); %>

  </div>
  <!-- /recent posts box -->
</div>